import { LocationType, StoreNameType } from '../@types';

export const LocationConst:readonly LocationType[] = ['config', 'home', 'store'];
export const StoreNameConst:readonly StoreNameType[] = ['bookmark', 'event_log'];
